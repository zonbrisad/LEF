#
#---------------------------------------------------------------------
# Lightweight Event Framework init script 
#    
#---------------------------------------------------------------------
#

##V Directory where script is located
#BP_SELF_DIR="${0%/*}"
INIT_DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )  

#---------------------------------------------------------------------

# Add script path to global path
export PATH=${PATH}:${INIT_DIR}
